# CAD to G-code Converter Requirements
# Install with: pip3 install -r requirements.txt

# For DXF file support (required for DXF import/export)
ezdxf>=1.0.0

# For SVG file support (optional)
svg.path>=1.5.0

# For font rendering in sign generation (optional)
fontTools>=4.0.0

# For testing (development)
pytest>=7.0.0

# For web application (optional - only needed for web version)
Flask>=2.3.0
Werkzeug>=2.3.0

# Note: tkinter comes with Python on macOS, Windows, and most Linux distributions
# On some Linux systems, you may need to install: sudo apt-get install python3-tk

